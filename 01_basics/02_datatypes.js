// Primitive Datatype

let name = "Rati"
let number = 9310088262
let boolean = true //false

// BigInt()
// Symbol
// null
let a = null
let b;

console.table([name,number,boolean,a,b])

// Non Primitive Datatype

// Object

console.table([typeof(name),typeof(number),typeof(boolean),typeof(a),typeof(b)])


// the type of null is an object
// the type of undefined is undefined